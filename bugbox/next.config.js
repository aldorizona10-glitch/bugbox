/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow the app to be deployed from a subdirectory on Vercel.
  // The app is served from the root of its own deployment.
};

module.exports = nextConfig;
